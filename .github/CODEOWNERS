# Toda alteração no catálogo precisa de revisão do dono do repositório.
#
# Para valer, combine com "Branch protection" em `main`:
#   GitHub → Settings → Branches → Add branch ruleset (ou Add rule) para `main`:
#     - Require a pull request before merging
#     - Require review from Code Owners
#     - Block force pushes
#     - Restrict deletions
#
# Assim, ninguém (nem colaborador) altera o código ou as fotos sem
# um Pull Request aprovado por @seolim2011-dotcom.

*       @seolim2011-dotcom
